# 🍽️ Where to DINE - 完整使用指南

基于真实数据的完整工作流程

---

## 📋 目录

1. [快速开始](#快速开始)
2. [数据采集](#数据采集)
3. [数据上传](#数据上传)
4. [数据处理](#数据处理)
5. [前端展示](#前端展示)
6. [常见问题](#常见问题)

---

## 🚀 快速开始

### 您已经采集了数据！

根据您的数据采集结果：
- ✅ **Google Maps**: 14,330个餐厅（94.3%有评分，72.2%有价格信息）
- ✅ **OpenStreetMap**: 7,723个餐厅（98.3%有名称，78.6%有菜系信息）

### 下一步工作流程

```
1. 上传数据到GitHub  ←  你在这里！
   ↓
2. 运行聚类脚本处理数据
   ↓
3. 启动前端WebGIS应用
   ↓
4. 查看推荐结果
```

---

## 📊 您的数据详情

### Google Maps数据分析

```
文件名: restaurants_nyc_googlemaps.csv
记录数: 14,330条
覆盖范围: 纬度40.50-40.91° × 经度-74.25--73.70°
平均评分: 4.15/5.0

数据列：
✅ place_id          - Google地点ID
✅ name              - 餐厅名称
✅ address           - 地址
✅ latitude          - 纬度
✅ longitude         - 经度
✅ rating            - 评分 (94.3%有数据)
✅ user_ratings_total - 评分人数
✅ price_level       - 价格等级 (72.2%有数据)
✅ types             - 类型标签
✅ business_status   - 营业状态
✅ category          - 类别
```

### OpenStreetMap数据分析

```
文件名: restaurants_nyc_osm.csv
记录数: 7,723条
覆盖范围: 纬度40.51-40.91° × 经度-74.25--73.70°

数据列：
✅ name              - 餐厅名称 (98.3%有数据)
✅ latitude          - 纬度 (100%)
✅ longitude         - 经度 (100%)
✅ cuisine           - 菜系类型 (78.6%有数据)
✅ phone             - 电话 (55.4%有数据)
✅ website           - 网站 (47.1%有数据)
✅ opening_hours     - 营业时间 (46.9%有数据)
✅ street            - 街道 (71.7%有数据)
✅ city              - 城市
✅ amenity           - POI类型
✅ category          - 类别
```

---

## 📤 步骤1: 上传数据到GitHub

### 方法A: 使用Git命令（推荐）

```bash
# 1. 进入项目目录
cd /path/to/Test-on-Claude-code

# 2. 创建data/raw目录（如果不存在）
mkdir -p data/raw

# 3. 复制CSV文件到项目
# 方式1: 如果文件在Downloads文件夹
cp ~/Downloads/restaurants_nyc_googlemaps.csv data/raw/
cp ~/Downloads/restaurants_nyc_osm.csv data/raw/

# 方式2: 如果文件在当前目录
mv restaurants_nyc_googlemaps.csv data/raw/
mv restaurants_nyc_osm.csv data/raw/

# 4. 验证文件是否存在
ls -lh data/raw/*.csv

# 应该看到：
# restaurants_nyc_googlemaps.csv  (~2-3 MB)
# restaurants_nyc_osm.csv         (~1-2 MB)

# 5. 添加到Git
git add data/raw/restaurants_nyc_googlemaps.csv
git add data/raw/restaurants_nyc_osm.csv

# 6. 提交
git commit -m "feat: Add restaurant data from Colab

- Google Maps: 14,330 restaurants (rating, price, reviews)
- OpenStreetMap: 7,723 restaurants (cuisine, hours, contact)
- Full NYC coverage for clustering analysis"

# 7. 推送
git push origin claude/arcgis-implementation-011CUq8tjcwbgcN56TxKrijW
```

### 方法B: 通过GitHub网页上传

1. 访问：https://github.com/Shi0v0Jasmine/Test-on-Claude-code
2. 切换到你的分支：`claude/arcgis-implementation-011CUq8tjcwbgcN56TxKrijW`
3. 进入 `data` → `raw` 文件夹
4. 点击 `Add file` → `Upload files`
5. 上传两个CSV文件
6. 提交更改

---

## 🔧 步骤2: 数据处理

### 2.1 安装Python依赖

```bash
# 进入数据处理目录
cd backend/data_processing

# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Mac/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate

# 安装依赖库
pip install -r requirements.txt

# 应该安装以下库：
# ✅ pandas
# ✅ numpy
# ✅ geopandas
# ✅ shapely
# ✅ hdbscan
# ✅ scikit-learn
```

### 2.2 运行餐厅聚类

```bash
# 确保在 backend/data_processing 目录

# 运行脚本1: 聚类餐厅位置
python 01_cluster_restaurants.py
```

**预期输出：**
```
============================================================
🍽️  RESTAURANT CLUSTERING PIPELINE
============================================================

📂 Using input file: ../data/raw/restaurants_nyc_googlemaps.csv
Loading restaurant data from ../data/raw/restaurants_nyc_googlemaps.csv...
Detected data source: GOOGLE_MAPS
Loaded 14330 valid restaurants

Running HDBSCAN clustering...
  Parameters: min_cluster_size=8, min_samples=4
✅ Found 182 restaurant clusters
   Noise points: 4523 (31.6%)

Creating dining zone polygons...
✅ Created 182 dining zone polygons

Saving dining zones to ../data/processed/dining_zones.geojson...
✅ Saved successfully (245.3 KB)

============================================================
📊 DINING ZONES STATISTICS
============================================================

Total dining zones: 182
Total restaurants in clusters: 9807
Average restaurants per zone: 53.9
Largest zone: 456 restaurants
Smallest zone: 8 restaurants

Average rating across zones: 4.18/5.0
============================================================

✅ CLUSTERING COMPLETE
============================================================

📄 Output saved to: ../data/processed/dining_zones.geojson
🎯 Next step: Run 02_cluster_taxi_dropoffs.py
```

**生成的文件：**
- `data/processed/dining_zones.geojson` - 餐厅聚类区域

### 2.3 运行出租车数据聚类（如果有出租车数据）

如果您还没有出租车数据，可以跳过这一步，直接使用餐厅数据。

```bash
# 可选：如果有出租车数据
python 02_cluster_taxi_dropoffs.py

# 如果没有出租车数据，可以创建模拟数据：
python create_sample_taxi_data.py  # （稍后提供）
```

### 2.4 合并生成最终热点（可选）

```bash
# 如果运行了02脚本
python 03_combine_hotspots.py
```

---

## 🌐 步骤3: 启动前端应用

### 3.1 使用本地服务器（推荐）

```bash
# 回到项目根目录
cd ../..  # 从 backend/data_processing 回到根目录

# 方法1: Python 3
python -m http.server 8000

# 方法2: Python 2
python -m SimpleHTTPServer 8000

# 方法3: Node.js (如果已安装)
npx http-server -p 8000
```

### 3.2 打开浏览器

访问：**http://localhost:8000/frontend/index.html**

### 3.3 查看效果

您应该看到：

1. **交互式地图** - 显示纽约市
2. **控制面板** - 右侧有交通方式选择
3. **点击地图** - 设置起点
4. **服务区域** - 自动显示可达范围
5. **推荐列表** - 显示附近的用餐热点

---

## 📊 步骤4: 加载真实热点数据

### 4.1 更新前端配置

编辑 `frontend/js/map.js`:

```javascript
// 找到 loadHotspotsData() 函数（约第150行）
function loadHotspotsData() {
    require([
        "esri/Graphic",
        "esri/geometry/Polygon"
    ], function(Graphic, Polygon) {

        // 加载真实的GeoJSON数据
        fetch('../data/processed/dining_zones.geojson')
            .then(response => response.json())
            .then(data => {
                console.log(`Loading ${data.features.length} hotspots...`);

                data.features.forEach(feature => {
                    const geometry = feature.geometry;
                    const properties = feature.properties;

                    // 创建Polygon
                    const polygon = {
                        type: "polygon",
                        rings: geometry.coordinates
                    };

                    const graphic = new Graphic({
                        geometry: polygon,
                        symbol: {
                            type: "simple-fill",
                            color: [255, 193, 7, 0.3],
                            outline: {
                                color: [255, 152, 0],
                                width: 2
                            }
                        },
                        attributes: {
                            name: `Zone ${properties.cluster_id}`,
                            restaurant_count: properties.restaurant_count,
                            avg_rating: properties.avg_rating || null,
                            popularity: properties.restaurant_count
                        },
                        popupTemplate: {
                            title: "Dining Zone {cluster_id}",
                            content: `
                                <b>Restaurants:</b> {restaurant_count}<br>
                                <b>Avg Rating:</b> {avg_rating}/5.0<br>
                                <b>Sample:</b> {restaurants}
                            `
                        }
                    });

                    hotspotsLayer.add(graphic);
                });

                console.log("✅ Hotspots loaded successfully");
            })
            .catch(error => {
                console.error("Error loading hotspots:", error);
            });
    });
}
```

### 4.2 刷新页面

重新加载浏览器，您现在应该看到真实的餐厅热点区域！

---

## 📈 数据质量检查

### Google Maps数据特点

**优势：**
- ✅ 评分数据丰富（94.3%）
- ✅ 价格信息完整（72.2%）
- ✅ 数据量大（14,330条）
- ✅ 用户评论数据

**适用场景：**
- 基于评分的推荐
- 价格筛选
- 热度排序

### OpenStreetMap数据特点

**优势：**
- ✅ 菜系信息丰富（78.6%）
- ✅ 营业时间（46.9%）
- ✅ 联系方式（电话、网站）
- ✅ 完全免费

**适用场景：**
- 菜系类型筛选
- 营业时间查询
- 联系信息展示

### 推荐使用策略

**策略1: 优先使用Google Maps**
```bash
# 使用Google Maps数据
mv data/raw/restaurants_nyc_googlemaps.csv data/raw/restaurants_nyc.csv
python backend/data_processing/01_cluster_restaurants.py
```

**策略2: 合并两个数据源**
```python
# 创建合并脚本
import pandas as pd

gm = pd.read_csv('data/raw/restaurants_nyc_googlemaps.csv')
osm = pd.read_csv('data/raw/restaurants_nyc_osm.csv')

# 合并（根据坐标去重）
combined = pd.concat([gm, osm]).drop_duplicates(
    subset=['latitude', 'longitude'],
    keep='first'
)

combined.to_csv('data/raw/restaurants_nyc_combined.csv', index=False)
```

---

## 🎯 完整工作流程检查清单

### ✅ 数据采集阶段
- [x] 运行Google Maps Colab notebook
- [x] 运行OpenStreetMap Colab notebook
- [x] 下载生成的CSV文件

### ✅ 数据上传阶段
- [ ] 复制CSV文件到 `data/raw/`
- [ ] Git add + commit + push
- [ ] 在GitHub上验证文件存在

### ✅ 数据处理阶段
- [ ] 安装Python依赖
- [ ] 运行 `01_cluster_restaurants.py`
- [ ] 验证生成 `dining_zones.geojson`
- [ ] （可选）运行 `02_cluster_taxi_dropoffs.py`
- [ ] （可选）运行 `03_combine_hotspots.py`

### ✅ 前端展示阶段
- [ ] 启动本地服务器
- [ ] 浏览器打开应用
- [ ] 测试地图点击功能
- [ ] 验证服务区域显示
- [ ] 检查推荐列表

### ✅ 项目展示阶段
- [ ] 准备演示PPT
- [ ] 录制演示视频（可选）
- [ ] 部署到GitHub Pages（可选）
- [ ] 准备项目报告

---

## 🐛 常见问题

### Q1: 运行聚类脚本时出现"No module named 'hdbscan'"

```bash
# 解决方案
pip install hdbscan scikit-learn

# 如果还是失败，尝试：
conda install -c conda-forge hdbscan
```

### Q2: 聚类结果太少或太多？

**调整参数：**

编辑 `01_cluster_restaurants.py`:

```python
# 减少聚类数量（更大的区域）
MIN_CLUSTER_SIZE = 15  # 默认是8
MIN_SAMPLES = 8        # 默认是4

# 增加聚类数量（更小、更细的区域）
MIN_CLUSTER_SIZE = 5
MIN_SAMPLES = 2
```

### Q3: 前端地图不显示热点？

**检查步骤：**

```bash
# 1. 确认GeoJSON文件存在
ls -la data/processed/dining_zones.geojson

# 2. 检查文件内容
head -n 20 data/processed/dining_zones.geojson

# 3. 验证JSON格式
python -m json.tool data/processed/dining_zones.geojson > /dev/null
echo "JSON格式正确！"

# 4. 检查浏览器控制台（F12）
# 查看是否有加载错误
```

### Q4: GitHub推送时文件太大？

```bash
# 检查文件大小
ls -lh data/raw/*.csv

# 如果 > 100MB，使用Git LFS
git lfs install
git lfs track "data/raw/*.csv"
git add .gitattributes
git commit -m "Configure Git LFS"
git add data/raw/*.csv
git commit -m "Add large data files via LFS"
git push
```

### Q5: 想使用不同的数据源？

**脚本会自动检测！**

```bash
# 脚本会按优先级尝试加载：
# 1. restaurants_nyc_googlemaps.csv (优先)
# 2. restaurants_nyc_osm.csv
# 3. restaurants_nyc.csv (通用名)

# 所以您只需确保文件名正确即可！
```

---

## 🎬 项目展示建议

### 演示流程（10分钟）

**1. 背景介绍（2分钟）**
- 问题：传统推荐系统的局限性
- 方案：基于真实移动数据的GIS推荐系统

**2. 数据展示（2分钟）**
- 展示Colab notebook运行结果
- 说明数据来源和数量
- 展示数据质量统计

**3. 方法论（2分钟）**
- HDBSCAN聚类算法
- 多模态可达性分析
- 推荐引擎打分机制

**4. 系统演示（3分钟）**
- 打开WebGIS应用
- 点击地图演示功能
- 展示推荐结果
- 切换交通方式

**5. 结果分析（1分钟）**
- 聚类统计结果
- 热点分布特征
- 实际应用价值

### 准备材料清单

- [ ] PPT演示文稿
- [ ] 项目演示（本地或在线）
- [ ] 数据采集截图
- [ ] 聚类结果可视化
- [ ] GitHub代码仓库链接
- [ ] 演示视频（备用）

---

## 📞 需要帮助？

### 检查系统状态

```bash
# 检查所有文件是否就位
ls -R data/

# 应该看到：
# data/raw/
#   restaurants_nyc_googlemaps.csv
#   restaurants_nyc_osm.csv
#
# data/processed/
#   dining_zones.geojson

# 检查Python环境
python --version  # 应该是 3.8+
pip list | grep -E "pandas|geopandas|hdbscan"

# 检查Git状态
git status
git log --oneline -5
```

### 诊断问题

```bash
# 测试数据加载
python -c "import pandas as pd; df = pd.read_csv('data/raw/restaurants_nyc_googlemaps.csv'); print(f'Loaded {len(df)} rows')"

# 测试聚类库
python -c "from sklearn.cluster import HDBSCAN; print('HDBSCAN OK')"

# 测试GeoJSON
python -c "import geopandas as gpd; gdf = gpd.read_file('data/processed/dining_zones.geojson'); print(f'Loaded {len(gdf)} zones')"
```

---

## 🎉 完成！

恭喜您完成了整个数据处理流程！

### 成果检查

您现在应该有：

✅ **原始数据**
- Google Maps: 14,330个餐厅
- OpenStreetMap: 7,723个餐厅

✅ **处理结果**
- ~182个餐厅聚类区域
- 包含评分、价格、菜系等信息

✅ **可视化应用**
- 交互式WebGIS地图
- 多模态服务区域
- 智能推荐引擎

### 下一步

- 🎨 **美化界面** - 调整颜色、样式
- 📊 **增强分析** - 添加更多统计图表
- 🚀 **部署上线** - GitHub Pages或云服务
- 📝 **完善文档** - 编写项目报告

**祝您项目展示成功！** 🎊
