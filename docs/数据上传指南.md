# 📤 数据上传到GitHub指南

## 问题：数据文件太大怎么办？

GitHub对单个文件有限制：
- ⚠️ 单文件 > 100 MB 会被拒绝
- ⚠️ 单文件 > 50 MB 会收到警告
- ⚠️ 仓库总大小建议 < 1 GB

您的餐厅数据（14,330 + 7,723条记录）CSV文件可能在 **2-5 MB** 左右，**可以直接上传**！

---

## 🎯 推荐方案：直接上传小数据文件

### 检查文件大小

```bash
# Mac/Linux
ls -lh restaurants_nyc_googlemaps.csv
ls -lh restaurants_nyc_osm.csv

# Windows (PowerShell)
Get-ChildItem restaurants_nyc_googlemaps.csv | Select-Object Name, Length
```

**如果文件 < 50 MB，直接上传即可！**

---

## 📤 方案1：通过Git命令行上传（推荐）

### 步骤：

```bash
# 1. 确保在项目根目录
cd /path/to/Test-on-Claude-code

# 2. 创建data/raw目录（如果不存在）
mkdir -p data/raw

# 3. 复制CSV文件到项目
# 从你的下载目录复制文件
cp ~/Downloads/restaurants_nyc_googlemaps.csv data/raw/
cp ~/Downloads/restaurants_nyc_osm.csv data/raw/

# 4. 检查文件是否在正确位置
ls -la data/raw/

# 5. 添加到Git
git add data/raw/restaurants_nyc_googlemaps.csv
git add data/raw/restaurants_nyc_osm.csv

# 6. 提交
git commit -m "feat: Add real restaurant data from Google Maps and OSM

- Google Maps: 14,330 restaurants with ratings and prices
- OpenStreetMap: 7,723 restaurants with cuisine info
- Coverage: Full NYC area (40.50-40.91°N, -74.25--73.70°W)
- Ready for clustering and analysis"

# 7. 推送到远程
git push origin claude/arcgis-implementation-011CUq8tjcwbgcN56TxKrijW
```

---

## 📤 方案2：通过GitHub网页界面上传

### 步骤：

1. **访问GitHub仓库**
   ```
   https://github.com/Shi0v0Jasmine/Test-on-Claude-code
   ```

2. **进入data/raw目录**
   - 点击 `data` 文件夹
   - 点击 `raw` 文件夹

3. **上传文件**
   - 点击 `Add file` → `Upload files`
   - 拖拽或选择文件：
     - `restaurants_nyc_googlemaps.csv`
     - `restaurants_nyc_osm.csv`

4. **填写提交信息**
   ```
   Title: Add restaurant data files

   Description:
   - Google Maps: 14,330 restaurants
   - OpenStreetMap: 7,723 restaurants
   ```

5. **提交**
   - 点击 `Commit changes`

---

## 📤 方案3：使用Git LFS（如果文件很大 > 50MB）

**只在文件超过50MB时使用！**

```bash
# 1. 安装Git LFS
# Mac
brew install git-lfs

# Windows
# 从 https://git-lfs.github.com/ 下载安装

# Ubuntu/Debian
sudo apt-get install git-lfs

# 2. 初始化Git LFS
git lfs install

# 3. 跟踪大文件
git lfs track "*.csv"
git lfs track "data/raw/*.csv"

# 4. 提交.gitattributes
git add .gitattributes
git commit -m "Configure Git LFS for CSV files"

# 5. 添加数据文件
git add data/raw/restaurants_nyc_googlemaps.csv
git add data/raw/restaurants_nyc_osm.csv

# 6. 提交并推送
git commit -m "Add restaurant data via Git LFS"
git push origin claude/arcgis-implementation-011CUq8tjcwbgcN56TxKrijW
```

---

## 📤 方案4：使用云存储（Google Drive/Dropbox）

**如果文件确实太大（>100MB）**

### 步骤：

1. **上传到云端**
   - 上传CSV文件到Google Drive或Dropbox
   - 设置为"任何人都可以查看"
   - 复制分享链接

2. **创建下载脚本**

创建文件 `data/download_real_data.sh`:

```bash
#!/bin/bash
# 下载真实餐厅数据

echo "📥 下载餐厅数据..."

# Google Drive下载（替换为你的文件ID）
# 从分享链接提取ID: https://drive.google.com/file/d/FILE_ID/view?usp=sharing

# 下载Google Maps数据
wget --no-check-certificate 'https://docs.google.com/uc?export=download&id=YOUR_GOOGLE_MAPS_FILE_ID' -O data/raw/restaurants_nyc_googlemaps.csv

# 下载OSM数据
wget --no-check-certificate 'https://docs.google.com/uc?export=download&id=YOUR_OSM_FILE_ID' -O data/raw/restaurants_nyc_osm.csv

echo "✅ 下载完成！"
```

3. **提交下载脚本到GitHub**
```bash
git add data/download_real_data.sh
git commit -m "Add data download script with cloud storage links"
git push
```

4. **在README中说明**
```markdown
## 数据下载

由于数据文件较大，请运行以下命令下载：

```bash
bash data/download_real_data.sh
```
```

---

## 🔍 验证上传成功

### 方法1：GitHub网页查看

访问：
```
https://github.com/Shi0v0Jasmine/Test-on-Claude-code/tree/claude/arcgis-implementation-011CUq8tjcwbgcN56TxKrijW/data/raw
```

应该能看到：
- `restaurants_nyc_googlemaps.csv`
- `restaurants_nyc_osm.csv`

### 方法2：Git命令行检查

```bash
# 查看远程文件
git ls-tree -r HEAD data/raw/

# 或者重新克隆测试
git clone https://github.com/Shi0v0Jasmine/Test-on-Claude-code.git test-clone
cd test-clone
ls -la data/raw/
```

---

## 📊 数据文件信息

### Google Maps数据 (restaurants_nyc_googlemaps.csv)

**文件大小预估：** ~2-3 MB

**列名：**
```
place_id, name, address, latitude, longitude, rating,
user_ratings_total, price_level, types, business_status, category
```

**示例数据：**
```csv
ChIJxxx,Joe's Pizza,123 Main St,40.7308,-74.0020,4.5,1234,2,restaurant|food,OPERATIONAL,restaurant
```

### OpenStreetMap数据 (restaurants_nyc_osm.csv)

**文件大小预估：** ~1-2 MB

**列名：**
```
name, latitude, longitude, cuisine, phone, website,
opening_hours, street, house_number, city, amenity,
brand, operator, category
```

**示例数据：**
```csv
Joe's Pizza,40.7308,-74.0020,pizza,+1-212-555-0100,http://example.com,Mo-Su 11:00-22:00,Main St,123,New York,restaurant,,,restaurant
```

---

## ⚠️ 常见问题

### Q1: 上传时显示文件太大？

**A:** 使用Git LFS（方案3）或云存储（方案4）

### Q2: 不想公开真实数据？

**A:** 有几个选择：

**选项1：创建采样数据**
```bash
# 只上传前1000条作为示例
head -n 1001 restaurants_nyc_googlemaps.csv > data/raw/restaurants_sample.csv
git add data/raw/restaurants_sample.csv
```

**选项2：使用.gitignore**
```bash
# 添加到.gitignore
echo "data/raw/restaurants_nyc_*.csv" >> .gitignore
git add .gitignore
git commit -m "Ignore raw restaurant data files"
```

**选项3：使用私有仓库**
- 将仓库设置为Private
- Settings → Danger Zone → Change visibility

### Q3: 上传后想删除怎么办？

```bash
# 删除文件
git rm data/raw/restaurants_nyc_googlemaps.csv
git commit -m "Remove large data file"
git push

# 如果已经推送，需要清理历史
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch data/raw/restaurants_nyc_googlemaps.csv" \
  --prune-empty --tag-name-filter cat -- --all

git push origin --force --all
```

---

## 💡 最佳实践建议

### 对于学生项目（推荐）：

**✅ 直接上传小数据文件**
- 文件 < 10 MB：直接上传，最方便
- 保持仓库简洁，便于分享

### 对于大型项目：

**✅ 使用混合方案**
- 上传采样数据（1000条）到GitHub
- 完整数据放云存储
- 提供下载脚本

### 对于商业项目：

**✅ 使用外部存储**
- 数据库（PostgreSQL + PostGIS）
- 云存储（AWS S3, Google Cloud Storage）
- 只在GitHub存代码

---

## 🎯 您的推荐方案

根据您的数据：
- Google Maps: 14,330条 ≈ 2-3 MB
- OSM: 7,723条 ≈ 1-2 MB
- **总计 < 5 MB**

**推荐：直接使用方案1（Git命令行）上传**

```bash
# 一键上传命令
cd Test-on-Claude-code
mkdir -p data/raw
cp ~/Downloads/restaurants_nyc_*.csv data/raw/
git add data/raw/
git commit -m "feat: Add restaurant data (14,330 from Google Maps, 7,723 from OSM)"
git push origin claude/arcgis-implementation-011CUq8tjcwbgcN56TxKrijW
```

**简单、快速、不需要额外工具！** ✨

---

## 📞 需要帮助？

如果上传遇到问题：

1. **检查文件大小**
   ```bash
   ls -lh data/raw/*.csv
   ```

2. **查看Git状态**
   ```bash
   git status
   git log --oneline -5
   ```

3. **测试连接**
   ```bash
   git remote -v
   git fetch origin
   ```

上传成功后，您就可以开始运行数据处理脚本了！🚀
